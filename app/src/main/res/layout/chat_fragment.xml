<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    android:id="@+id/chat_fragment"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:id="@+id/bt_send_button"
        android:text="@string/send"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"/>

    <EditText
        android:id="@+id/et_input_chat"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_toLeftOf="@id/bt_send_button"
        android:layout_toStartOf="@id/bt_send_button"
        android:layout_alignParentBottom="true"
        android:inputType="text"
        android:hint="@string/et_message_hint"/>

    <ListView
        android:id="@+id/chat_list_view"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_above="@id/bt_send_button"/>

</RelativeLayout>